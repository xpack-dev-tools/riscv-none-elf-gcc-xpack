
{/* ------------------------------------------------------------------------ */}

{/* Below 5 minutes use "Several minutes later" */}
export const developmentDurations = {
    "work": "about 162 minutes (2h42m) - when using the multi-libs subset -",
    "darwin-x64": "About 97 minutes (1h37m) later (when using the multi-libs subset)",
    "darwin-arm64": "About 55 minutes later (when using the multi-libs subset)",
    "linux-x64": "About 62 minutes (1h02m) later (when using the multi-libs subset)",
    "linux-arm64": "About 110 minutes (1h50m) later (when using the multi-libs subset)",
    "win32-x64": "About 46 minutes later (when using the multi-libs subset)"
}
